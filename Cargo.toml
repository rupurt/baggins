[package]
name = "baggins"
version = "0.1.0"
edition = "2021"

[lib]
name = "baggins"
path = "src/lib.rs"

[dependencies]

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(test)'] }
