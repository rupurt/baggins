# Lightweight, scaffold-safe tasks for the current repository state.

default:
  @just --list

test:
  @echo "No tests defined yet; treating scaffold as passing."

quality:
  @echo "No quality checks defined yet; treating scaffold as passing."
